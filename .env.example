# ============================================
# VCET ALUMNI HUB - ENVIRONMENT VARIABLES
# ============================================
# For Local Development: Copy to .env
# For Vercel: Add these in Project Settings → Environment Variables
# ============================================

# --------------------------------------------
# SERVER CONFIGURATION
# --------------------------------------------
NODE_ENV=production
PORT=3000
HOST=localhost

# --------------------------------------------
# DATABASE CONFIGURATION
# --------------------------------------------
# For Vercel, use a cloud MySQL service like:
# - PlanetScale (recommended for Vercel)
# - AWS RDS
# - DigitalOcean Managed MySQL
# - Railway
DB_HOST=your-mysql-host.com
DB_USER=your_mysql_username
DB_PASSWORD=your_mysql_password
DB_NAME=vcet_alumni_hub
DB_PORT=3306
DB_CONNECTION_LIMIT=10

# --------------------------------------------
# SESSION CONFIGURATION
# --------------------------------------------
# Generate a random string (min 32 characters)
# Use: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-change-this-to-random-string-min-32-chars
SESSION_MAX_AGE=86400000

# --------------------------------------------
# JWT CONFIGURATION
# --------------------------------------------
# Generate a random string (min 32 characters)
JWT_SECRET=your-jwt-secret-key-change-this-to-random-string-min-32-chars
JWT_EXPIRES_IN=7d

# --------------------------------------------
# APPLICATION CONFIGURATION
# --------------------------------------------
APP_NAME=VCET Alumni Hub
APP_VERSION=1.0.0
# Update with your Vercel deployment URL
BASE_URL=https://your-app-name.vercel.app

# --------------------------------------------
# FILE UPLOAD CONFIGURATION
# --------------------------------------------
MAX_FILE_SIZE=5242880
ALLOWED_EXTENSIONS=jpg,jpeg,png
# For Vercel, use /tmp for uploads (temporary)
# For persistent uploads, use Cloudinary or similar
UPLOAD_DIR=./uploads

# --------------------------------------------
# SECURITY CONFIGURATION
# --------------------------------------------
BCRYPT_ROUNDS=10

# ============================================
# VERCEL DEPLOYMENT NOTES:
# ============================================
# 
# 1. Add environment variables in Vercel Dashboard:
#    Settings → Environment Variables
# 
# 2. Required variables:
#    - All DB_* variables
#    - SESSION_SECRET
#    - JWT_SECRET
#    - BASE_URL (your Vercel URL)
# 
# 3. Database setup:
#    - Use PlanetScale (free tier, serverless-friendly)
#    - Or any cloud MySQL provider
#    - Import database-export.sql to your cloud database
# 
# 4. File uploads:
#    - Vercel serverless functions have ephemeral storage
#    - Use Cloudinary for persistent file uploads
#    - Or update code to use /tmp and warn users
# 
# ============================================
